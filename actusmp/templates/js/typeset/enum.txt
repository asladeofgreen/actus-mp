/**
 *  N.B. Auto-generated using actus-mp
 */

/**
 *  {{definition.acronym}} :: {{definition.name}}.
 *
 *  {{definition.description}}
 */
export enum {{utils.to_camel_case(definition.identifier)}} {
{% for member in definition.allowed_values %}
    // {{member.name}}: {{member.description.strip()}}
    {{utils.to_ts_enum_member(member)}} = {{member.option}},
{% if member != definition.allowed_values[-1] %}

{% endif %}
{% endfor %}
}

