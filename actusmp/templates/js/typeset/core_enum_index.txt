/**
 *  N.B. Auto-generated using actus-mp
 */

{% for defn in dictionary.enum_set_core %}
import { {{utils.to_camel_case(defn.identifier)}} } from './{{utils.to_pascal_case(defn.identifier)}}';
{% endfor %}

export {
{% for defn in dictionary.enum_set_core %}
    {{utils.to_camel_case(defn.identifier)}},
{% endfor %}
}

export const ENUM_SET = new Set<object>([
{% for defn in dictionary.enum_set_core %}
    {{utils.to_camel_case(defn.identifier)}},
{% endfor %}
]);

