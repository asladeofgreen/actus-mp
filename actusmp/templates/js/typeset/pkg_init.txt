/**
 *  N.B. Auto-generated using actus-mp
 */

import * as core from './core';

import {
    ENUM_SET,
{% for defn in dictionary.enum_set %}
    {{utils.to_camel_case(defn.identifier)}},
{% endfor %}
} from './enums';

import {
    CONTRACT_TERMSETS,
{% for defn in dictionary.contract_set %}
    TermsetOf{{utils.to_camel_case(defn.type_info.identifier)}},
{% endfor %}
} from './terms';

export {
    // Core.
    core,
    // Enumerations.
    ENUM_SET,
{% for defn in dictionary.enum_set %}
    {{utils.to_camel_case(defn.identifier)}},
{% endfor %}
    // Termsets.
    CONTRACT_TERMSETS,
{% for defn in dictionary.contract_set %}
    TermsetOf{{utils.to_camel_case(defn.type_info.identifier)}},
{% endfor %}
}

